sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t cosimogiosa/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true cosimogiosa/docker-react npm run test
